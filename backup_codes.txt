import 'package:flutter/material.dart';
import 'dart:io';

class PublishWorkScreen extends StatefulWidget {
  @override
  _PublishWorkScreenState createState() => _PublishWorkScreenState();
}

class _PublishWorkScreenState extends State<PublishWorkScreen> {
  String status = "مستمر";
  bool isUploading = false;
  double uploadProgress = 0.0;

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("لوحة تحكم الناشر - SAMAQ")),
      body: Column(
        children: [
          Expanded(
            child: SingleChildScrollView(
              padding: EdgeInsets.all(16),
              child: Column(
                children: [
                  // --- خانات المعلومات ---
                  TextField(decoration: InputDecoration(labelText: "اسم المانهوا", border: OutlineInputBorder())),
                  SizedBox(height: 15),
                  DropdownButtonFormField(
                    value: status,
                    items: ["مستمر", "مكتمل", "متوقف"].map((e) => DropdownMenuItem(child: Text(e), value: e)).toList(),
                    onChanged: (val) => setState(() => status = val.toString()),
                    decoration: InputDecoration(labelText: "حالة العمل", border: OutlineInputBorder()),
                  ),
                  SizedBox(height: 25),

                  // --- زر رفع الفصول (حتى 50 صورة) ---
                  ElevatedButton.icon(
                    style: ElevatedButton.styleFrom(minimumSize: Size(double.infinity, 60), backgroundColor: Colors.blue[900]),
                    icon: Icon(Icons.collections),
                    label: Text("اختيار الصور (حد أقصى 50)"),
                    onPressed: () {
                      // كود الـ Image Picker سيوضع هنا
                      ScaffoldMessenger.of(context).showSnackBar(SnackBar(content: Text("تم فتح الاستوديو...")));
                    },
                  ),

                  if (isUploading) ...[
                    SizedBox(height: 20),
                    Text("جاري وضع العلامة المائية والرفع..."),
                    LinearProgressIndicator(value: uploadProgress),
                  ],
                ],
              ),
            ),
          ),
          
          // --- قسم الإعلانات (يظهر دائماً في الأسفل) ---
          Container(
            width: double.infinity,
            height: 60,
            color: Colors.black,
            child: Center(child: Text("مساحة إعلانية - AdMob", style: TextStyle(color: Colors.grey))),
          ),
        ],
      ),
    );
  }
}
// --- كود قالب الإعلانات SAMAQ Ads ---
// هذا الكود يوضع داخل الـ Scaffold في أي صفحة تريدها

Widget samaqAdBanner() {
  return Container(
    height: 60, // ارتفاع الإعلان
    width: double.infinity,
    decoration: BoxDecoration(
      color: Colors.black, // خلفية الإعلان
      border: Border(top: BorderSide(color: Colors.blue, width: 0.5)), // خط جمالي أزرق
    ),
    alignment: Alignment.center,
    child: Row(
      mainAxisAlignment: MainAxisAlignment.center,
      children: [
        Icon(Icons.ads_click, color: Colors.blue, size: 20),
        SizedBox(width: 10),
        Column(
          mainAxisAlignment: MainAxisAlignment.center,
          crossAxisAlignment: CrossAxisAlignment.start,
          children: [
            Text("مساحة إعلانية مدعومة", style: TextStyle(color: Colors.white, fontSize: 10)),
            Text("إعلانات SAMAQ لدعم المترجمين", style: TextStyle(color: Colors.blue, fontSize: 12, fontWeight: FontWeight.bold)),
          ],
        ),
      ],
    ),
  );
}
import 'package:flutter/material.dart';

class MangaReaderScreen extends StatefulWidget {
  final String title;
  MangaReaderScreen({required this.title});

  @override
  _MangaReaderScreenState createState() => _MangaReaderScreenState();
}

class _MangaReaderScreenState extends State<MangaReaderScreen> {
  // هنا نضع روابط الصور التجريبية (لاحقاً ستكون صورك المرفوعة)
  final List<String> pages = [
    "https://via.placeholder.com/600x900.png?text=SAMAQ+Page+1",
    "https://via.placeholder.com/600x900.png?text=SAMAQ+Page+2",
    "https://via.placeholder.com/600x900.png?text=SAMAQ+Page+3",
  ];

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.black, // أسود حتى تبرز ألوان المانهوا
      appBar: AppBar(
        title: Text(widget.title),
        backgroundColor: Colors.blue[900]?.withOpacity(0.5),
      ),
      body: ListView.builder(
        itemCount: pages.length,
        itemBuilder: (context, index) {
          return InteractiveViewer( // يخلي المتابع يقدر يسوي زووم (Zoom) على الصفحة
            child: Image.network(
              pages[index],
              loadingBuilder: (context, child, progress) {
                if (progress == null) return child;
                return Center(child: CircularProgressIndicator());
              },
            ),
          );
        },
      ),
      // زر يظهر بالأسفل للإعلانات أو الانتقال للفصل القادم
      bottomNavigationBar: Container(
        height: 60,
        color: Colors.blue[900],
        child: Row(
          mainAxisAlignment: MainAxisAlignment.spaceAround,
          children: [
            IconButton(icon: Icon(Icons.arrow_back_ios), onPressed: () {}),
            Text("الفصل 1", style: TextStyle(color: Colors.white, fontWeight: FontWeight.bold)),
            IconButton(icon: Icon(Icons.arrow_forward_ios), onPressed: () {}),
          ],
        ),
      ),
    );
  }
}
